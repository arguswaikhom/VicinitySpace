<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.VicinitySpace" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_variant</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <item name="android:background">@color/background_color</item>
    </style>

    <style name="Theme.VicinitySpace.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ErrorTextStyle">
        <item name="android:fontFamily">sans-serif-thin"</item>
        <item name="android:textColor">@color/error_color_text</item>
        <item name="android:textSize">@dimen/error_text_size</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="TempTextStyle">
        <item name="android:fontFamily">sans-serif-black"</item>
        <item name="android:textColor">@color/temp_color_text</item>
        <item name="android:textSize">@dimen/temp_text_size</item>
    </style>

    <style name="LocationTextStyle">
        <item name="android:fontFamily">sans-serif-thin"</item>
        <item name="android:textColor">@color/location_color_text</item>
        <item name="android:textSize">@dimen/location_text_size</item>
    </style>

    <style name="ForecastItemStyle">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:fontFamily">sans-serif"</item>
        <item name="android:textColor">@color/forecast_color_text</item>
        <item name="android:textSize">@dimen/forecast_text_size</item>
    </style>

</resources>